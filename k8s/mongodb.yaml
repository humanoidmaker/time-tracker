apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb
  namespace: time-tracker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb
  template:
    metadata:
      labels:
        app: mongodb
    spec:
      containers:
      - name: mongodb
        image: mongo:7
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: mongo-storage
          mountPath: /data/db
      volumes:
      - name: mongo-storage
        persistentVolumeClaim:
          claimName: mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: time-tracker
spec:
  selector:
    app: mongodb
  ports:
  - port: 27017
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  namespace: time-tracker
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
